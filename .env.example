# Power users running outside the notebook can set these instead of arena.configure().
# The backend/leaderboard/data URLs default to the live workshop services, so
# usually you only need your team token.
ARENA_TOKEN=wksp_teamNN_xxxxxxxx
# ARENA_BACKEND_URL=https://...modal.run
# ARENA_LEADERBOARD_URL=https://...modal.run
# ARENA_DATA_URL=https://...modal.run/data
